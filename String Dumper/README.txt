Takes in a filename from command line and traverses bytes to find strings of printable characters .

Printable character = ASCII >= 32 && <= 126 as well as 9 (tab)

Strings are runs of four consecutive printable characters

> gcc -o mystrings mystring.c
> ./mystrings example.txt

Use README.txt as convenient example
Use mystrings executable for a valuable example 
